# Local desktop/demo profile.
APP_MODE="desktop-demo"
DATABASE_URL="file:./prisma/dev.db"
# Electron sets this automatically to app.getPath("userData")/user-media.
# DESKTOP_MEDIA_DIR="C:/path/to/persistent/user-media"

# Kept unset in desktop-demo on purpose. The local demo must not silently
# depend on an online API.
# API_BASE_URL=""
